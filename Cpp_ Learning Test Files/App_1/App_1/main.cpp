#include <iostream>
#include <string>
#include <sstream>
using namespace std;


int main(){
	/*These are multiple ways of using the standard output stream And this is how we use the multiLine comment.
	The system("pause") Invokes the command processor to execute a command, such as copy, move, whoami..etc
	std means the standard library
	*/
	/*cout << "Hello Walid\n"; cout << 13 << "\n\n";//printing a blank line
		cout << "WALID " << "is " << "The MAN" << endl;
	
	//system("copy C:\\Users\\%username%\\Desktop\\1\\walidImage.jpg C:\\Users\\%username%\\Desktop\\2\\walidImage.jpg");
	
		//string name;
		//getline(cin, name);//the "cin way" for String to be scanned
		cout << "your name is " << name<<endl;//you can use << for further text
		cout << "your name is "+name+ "\n";// or you can also use the "+"

		int a;
		int b;
		cin >> a>> b;//Any kind of space is used to separate two consecutive input operations; this may either be a space, a tab, or a new-line character.
		cout << a;
		
	string snumb= "100";
	int myint;
	stringstream(snumb)>>myint;//convert a string to a number
	cout << myint + 1 << endl;

	//If Statements
	int a, b;
	cin >> a>> b;

	if (a>b){
	cout << "a is greater than b \n";
	}
	else if (a<b){
	cout << "a is less than b \n";

	}
	else {
	cout << "a and b are equal \n";
	exit;
	}
	
	system("pause	*/

	return 0;
}

